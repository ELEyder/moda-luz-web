habilitar (si usas xampp) en php.ini
extension=zip

composer install

crear database modaluz
CREATE DATABASE modaluz

ejecutar script sql de app_data/db/ModaLuz.sql

cambiar el archivo .env por los datos de la base de datos